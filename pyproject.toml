[project]
name = "obsidian-mcp"
version = "0.1.0"
description = "quick mcp server integration im making to help me learn compilers"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.26.0",
    "pyyaml>=6.0.3",
]
